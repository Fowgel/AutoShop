@model IEnumerable<AutoWebShop.Models.CarShopModel>

<div class="container">
    @*<div class="row">
        @if (Model(x => x.Fileings) != null)
        {
            foreach (var item in Model)
            {
                <p>Hej</p>
                <a href="#"><img src="@Url.Action("ShowImage", new {fileName = item.Fileings})" /></a>

            }
        }
        else
        {
            <p>Kunde inte hitta filerna.</p>
        }

    </div>*@
    <div class="row">
        @foreach (var item in Model)
        {
        <p>Hej</p>
        <a href="#"><img src="@Url.Action("ShowImage", new {fileName = item.Fileings})" /></a>

        }
    </div>
</div>